import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Appointment from './containers/AppointmentContainer'

ReactDOM.render(
  <React.StrictMode>
    <Appointment />
  </React.StrictMode>,
  document.getElementById('root')
);
